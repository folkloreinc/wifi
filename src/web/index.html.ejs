<!DOCTYPE html>
<html>
    <head>
        <title>Wifi</title>
        <script type="text/javascript">
            var props = <%= "\<%-fromServer%\>" %> === true ? {
                locale: <%= "\<%=locale%\>" %>,
                translations: <%= "\<%-JSON.stringify(translations)%\>" %>,
                online: <%= "\<%=online ? 'true' : 'false'%\>" %>,
                networks: <%= "\<%-JSON.stringify(networks)%\>" %>
            } : {};
        </script>
    </head>
    <body>
        <div id="app"></div>
    </body>
</html>
